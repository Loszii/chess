#include <gtest/gtest.h>
#include <vector>
#include <array>

//When recursively calling moves, store temporary variables for things like promotion and castling,
//after calling undo move we also will reset them in the board class

TEST(Game, Perft) {
    //to do




    //function call example:

    //make_move()
    //update_board()

    //do something

    //undo_move()
    //undo_update_board()
}